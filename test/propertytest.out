Circuits `#lhs` and `#rhs` are defined.

When property testing `nand(X, Y) = nand(Y, X)`:

Equivalence between `lhs`, `rhs`:
  `lhs` behaves like `rhs`
    `{}` ~ `{}`

Circuits `not`, `#lhs`, and `#rhs` are defined.

When property testing `!!X = X`:

Equivalence between `lhs`, `rhs`:
  `lhs` behaves like `rhs`
    `{}` ~ `{}`

Circuits `or`, `#lhs`, and `#rhs` are defined.

When property testing `(X | Y) | Z = X | Y | Z`:

Equivalence between `lhs`, `rhs`:
  `lhs` behaves like `rhs`
    `{}` ~ `{}`

Circuits `#lhs` and `#rhs` are defined.

When property testing `X = !X`:

Equivalence between `lhs`, `rhs`:
  `lhs` has a behaviour that `rhs` does not match
  `lhs(0) = 0` but `rhs(0) = 1`
  `lhs(1) = 1` but `rhs(1) = 0`
